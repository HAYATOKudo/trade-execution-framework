from __future__ import annotations
from interface import Signal

def decide_signal(ctx: dict) -> Signal:
    # PUBLIC STUB: replace with your private strategy in a separate private repo.
    return Signal(action="NO_TRADE", reason="public stub")
